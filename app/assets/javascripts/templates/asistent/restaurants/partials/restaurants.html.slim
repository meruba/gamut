.restaurants-wrapper
  .title Establecimientos
  .restaurants
    .restaurant(ng-repeat="restaurant in vm.restaurants" ng-click="vm.selectRestaurant(restaurant)")
      .logo
        .img-circle
          image-upload options="{image: restaurant.logo, identicon: restaurant.email}"
      .description
        .name {{restaurant.name}}
